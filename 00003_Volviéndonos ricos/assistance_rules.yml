---
rules:
  - !ruby 
    when: !ruby
      these_expectations_failed:
        - "* DeclaresComputationWithArity0:aumentarFortuna"
    then: tienes que declarar un procedimiento que no tenga parámetros
  - !ruby 
    when: !ruby
      these_expectations_failed:
        - "* Not:SourceContains:return"
    then: recuerda que sólo tienes que asignar la variable global; no uses `return` porque no hay que _devolver_ nada
